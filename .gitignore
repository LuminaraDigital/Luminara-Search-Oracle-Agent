# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment / Worker secrets (never commit real values)
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example

# Credential files and keys
*.pem
*.p12
*.pfx
*.key
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
id_ecdsa
id_ecdsa.pub
*credentials*.json
*service_account*.json
*service-account*.json
google-api.json
application_default_credentials.json
**/application_default_credentials.json
.secrets/
secrets/
*.secret

# MCP configs often embed tokens; keep examples only if committed under docs
mcp.json
.mcp.json
.cursor/mcp.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local backups / scratch
*.backup
*.backup2
*.bak
.wrangler/
tmp/

# Vendored third-party repositories (reference only, each has its own .git)
/open-seo/
/Switchyard/
scratch/

# Internal planning docs (not part of the public release)
docs/internal/

# Local tooling
.claude/*
!.claude/skills/
!.claude/agents/
coverage/
